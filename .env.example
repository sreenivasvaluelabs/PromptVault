# Database Configuration
# For local development with in-memory storage, this is optional
# For production with PostgreSQL, set this to your database connection string
DATABASE_URL=postgresql://localhost:5432/promptvault
